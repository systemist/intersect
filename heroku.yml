setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile.web
    benthos: Dockerfile.benthos
run:
  web: bundle exec puma -C config/puma.rb
